<div class="relative">
  <div class="absolute inset-0 bg-cover bg-center filter hue-rotate-30" style="background-image: url(<%= asset_path 'bg_user.jpg' %>) "></div>
  <!-- This is the overlay -->
  <div class="relative z-10 flex justify-center items-center h-[calc(100vh-80px)]">
    <div class="container mx-auto max-w-92 grid gap-3 bg-white p-4">
      <%= yield %>
    </div>
  </div>
  <div class="z-0 absolute inset-0 bg-black/80"></div>
</div>
