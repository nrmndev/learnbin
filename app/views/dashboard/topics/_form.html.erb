<%= simple_form_for [@category, @topic] do |f| %>
  <%= f.input :name %>
  <%= f.input :slug %>
  <%= f.input :description %>
  <%= f.input :category_id, collection: Category.all, label_method: :name, input_html: { disabled: @topic.persisted? } %>
  <%= f.submit %>
<% end %>
